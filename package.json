{
  "name": "ueye",
  "version": "1.1.6",
  "description": "FFI-based Binding for IDS uEye camera library",
  "main": "lib/camera",
  "scripts": {
    "build-calibration": "(mkdir -p build; cd build && cmake .. && make) || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=6"
  },
  "author": "FreshX GmbH",
  "license": "ISC",
  "dependencies": {
    "co": "^4.6.0",
    "ffi-napi": "^2.4.5",
    "q": "^1.5.0",
    "ref-array-napi": "^1.2.0",
    "ref-napi": "^1.4.3",
    "ref-struct-napi": "^1.1.0",
    "ref-wchar-napi": "^1.0.2"
  }
}
